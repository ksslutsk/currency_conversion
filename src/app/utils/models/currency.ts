export interface Currency {
    exchangedate?: string,
    r030?: number,
    cc?: string,
    enname?: string,
    rate?: number,
    units?: number,
    rate_per_unit?: number,
    group?: string,
    calcdate?: string
}